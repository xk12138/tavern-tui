# 酒馆 · 使用文档

> 这份文档教你把 Tavern 装起来、配上一个大模型、走进第一个世界、开始第一段故事。
> 如果你想学**怎么造一个世界**,请看 `WORLD_BUILDING.md`。
> 如果你想学**引擎的架构与设计**,请看 `DESIGN.md`。

---

## 一、快速上手(5 分钟)

```bash
# 1. 安装
uv tool install tavern
# 或者
pipx install tavern

# 2. 启动
tavern
```

首次启动,Tavern 会依次问你三件事:
1. **选一个 LLM provider**,填 API key
2. **加载或创建一个世界**(可选:导入示范世界包)
3. **创建你的角色**

然后你就推开酒馆的门,故事开始了。

---

## 二、安装

### 系统要求
- Python 3.11+
- 一个能连上大模型 API 的网络环境(或本地跑 Ollama)
- 支持 ANSI 色彩的终端(macOS Terminal / iTerm2 / Windows Terminal / tmux 都行)

### 推荐:用 uv 安装

```bash
uv tool install tavern
```

优点:全局命令,和你的 Python 环境隔离,更新方便。

### 备选:用 pipx 安装

```bash
pipx install tavern
```

### 开发者:从源码安装

```bash
git clone https://github.com/<org>/tavern
cd tavern
uv sync
uv run tavern
```

---

## 三、配置 LLM

Tavern **不预置任何 LLM 后端**,你自选。

首次启动会引导你填 API key,写入到 `~/.config/tavern/config.toml`。你也可以随时手动编辑这个文件。

### 3.1 支持的 provider

| Provider | 说明 |
|---|---|
| `anthropic` | Claude 系列。叙事质量优秀,推荐用于 Narrator |
| `openai` | GPT 系列 |
| `deepseek` | 便宜大碗,适合 Extractor 这类不吃创作力的角色 |
| `ollama` | 本地部署,免费但需要显卡 |
| `custom` | 任何 OpenAI 兼容的接口 |

### 3.2 配置文件结构

`~/.config/tavern/config.toml`:

```toml
# 默认 provider:Narrator/Director/Memory Keeper 都会用它
[llm.default]
provider = "anthropic"
model    = "claude-sonnet-5"
api_key  = "sk-ant-..."

# 可选:让 Extractor 用更便宜的模型省钱
# 每回合都会调 Extractor 一次,成本敏感
[llm.extractor]
provider = "deepseek"
model    = "deepseek-chat"
api_key  = "sk-..."

# 可选:让 Director 用更强的模型,策划质量更高
# 低频调用,贵一点也可接受
# [llm.director]
# provider = "anthropic"
# model    = "claude-opus-5"
# api_key  = "sk-ant-..."

[ui]
typewriter_speed_ms = 20      # 打字机效果的每字符间隔
color_scheme        = "default"
```

**只有 `[llm.default]` 是必填的**,其他角色不配则复用 default。

### 3.3 用本地 Ollama

```toml
[llm.default]
provider = "ollama"
model    = "qwen2.5:14b"
base_url = "http://localhost:11434"
# api_key 留空
```

先起 Ollama:`ollama serve` + `ollama pull qwen2.5:14b`。

### 3.4 用自定义 OpenAI 兼容接口

任何符合 OpenAI 协议的服务(如某些内网代理、One-API):

```toml
[llm.default]
provider = "custom"
model    = "gpt-4o-mini"
api_key  = "sk-..."
base_url = "https://your-endpoint/v1"
```

---

## 四、你的第一个故事

### 4.1 选一个世界

启动 `tavern`,你会看到"元酒馆"世界选择界面:

```
        欢迎来到酒馆。

        你想去哪个世界?

        [1] (还没有任何世界。按 N 创建,按 I 导入)

        N  从零创建一个新世界
        I  导入一个世界包 (.toml 或目录)
        Q  退出
```

**从零创建**:进入引导式流程,让 LLM 帮你把一句话扩展成完整的世界设定(见 `WORLD_BUILDING.md`)。

**导入世界包**:如果你从社区下载了一个 `.toml` 或整个世界包目录:

```bash
tavern install ./my-world.toml
# 或者
tavern install ~/Downloads/some-world/
```

装完之后再启动 `tavern`,列表里就会出现。

### 4.2 创建你的角色

选好世界之后,系统会引导你建角色:

```
        你在这个世界里,是谁?

        [1] 从模板选一个 (推荐)
        [2] 自由描述我的角色
```

**模板路径**(3 分钟开局的默认路径):世界包通常自带 3~5 个人设模板(比如江湖世界的"落魄书生""游侠""酒楼小二"),挑一个,微调一句背景就能开局。

**自由描述**:一段自然语言,LLM 帮你补齐属性、软标签、初始物品。

### 4.3 推门入店

角色确定后,故事从**初始酒馆**开始。GM 会做三件事:
- 用感官描写把你放进场景(酒香、光线、周围的人声)
- 抛出**第一个钩子** —— 一个人、一件事、一个选择
- 停下,等你输入

从这里开始,你想干什么就干什么。

---

## 五、怎么玩(输入语法)

Tavern 的输入是**自由文本**,但用几个前缀可以让 GM 更好地理解你的意图。

| 输入 | 含义 | 例子 |
|---|---|---|
| 纯文本 | 自由行动 / 混合行为 | `我走到吧台,点了一壶酒` |
| `"..."` | 纯台词 | `"老板,来一壶最好的酒。"` |
| `*...*` | 心理活动 / 独白 | `*这人看我的眼神不对劲*` |
| `/xxx` | 系统指令,不进入叙事 | `/save` |
| `:xxx` | 快捷动作 | `:look` `:wait` |
| `[1]` `[2]` `[3]` | 直接采用建议列表里的一条 | 见 5.2 |

### 5.1 你可以做什么

**任何事**。真的。以下都是合法输入:

```
我拔剑
"你是谁?"
*我不能相信她说的话*
观察这个人的手,看有没有老茧
装作醉倒,偷听他们对话
翻这个抽屉
挑衅他,想看看他会不会动手
```

GM 会根据世界规则、你的属性、当前情境判定后果。**日常互动不需要骰子;关键行动**(生死、剧情节点、明显超出能力范围)会触发一次检定。

### 5.2 迷茫时:建议列表

**从你推开酒馆门的第一刻起,建议就已经就位** —— 引擎会读开局场景(opening hook)先给你推荐一轮,GM 每次回复后也会刷新。想不出该做什么时,看 GM 回复下方那组建议 —— 那是替你想好的、**玩家第一人称**的台词:

```
  [1] "老板,来杯酒"              ← 绿色 = 当前选项
  [2] 打量这间酒馆
  [3] 说点什么…                   ← 浅灰 = 自己说
```

- **↑ / ↓ 方向键**移动绿色高亮,**回车**采用当前选项 —— 效果**等于你自己输入了这句话**
- **直接打字,列表不会消失**:第一个字符按下时,高亮自动跳到末位 `[3] 说点什么…`(变绿),你的字出现在 `>` 提示行里 —— 列表会一直显示,**按回车才提交**
- **回车规则**:只要提示行里有字,回车就提交你打的字;没打字的回车才采用当前高亮选项
- 退格(⌫)编辑输入;不方便用方向键时(管道输入等),直接输入 `[1]` / `[2]` / `[3]`(或 `:1`/`:2`/`:3`)也能选
- 想看新的:`/hint`;想关掉:`/hint off`(再开 `/hint on`);永久关闭:config.toml 写 `[ui] suggestions = false`

### 5.3 无效或不合世界的行为

如果你想做的事情**违反世界规则**(比如在无魔法世界里放火球术),GM 会以叙事方式告诉你为什么做不到,或者给一个失败结果。GM **不会**跳出叙事说"这不合规则",一切都在故事里发生。

---

## 六、系统指令(斜杠命令)

以 `/` 开头,不会进入故事叙述。在输入框里输入 `/` 会自动弹出补全。

### 存档

```
/save [name]       存档。省略 name 则覆盖当前存档
/load [name]       载入
/saves             列出所有存档
/rewind            回退上一回合(手滑撤销;死亡后不可用)
```

**关于 rewind**:仅用于"输错了想收回"。不要指望它救命 —— 一旦 HP 归零进入死亡分支,rewind 不可用,你只能读档或回到酒馆重开。

### 查看当前状态

```
/who               当前场景在场的所有人物
/who <name>        某个 NPC 的已知信息(仅显示你已发现的内容)
/where             当前地点描述
/inv               我的物品栏
/status            我的完整属性(HP、金钱、软标签、精神值)
/relations         与主要 NPC 的关系(好感/信任/恐惧)
/journal           剧情日志:已发生的关键事件
/wiki <topic>      查看世界百科条目
```

### 场景

```
/tavern            回到初始酒馆(软性锚点,不消耗任何东西)
```

用于"这一段太乱了,我想去个熟悉的地方喘口气"。系统会给你一段过渡叙事把你带回酒馆。

### 导出

```
/export novel [path]     把当前进度重写为第三人称小说文本
```

省略 path 则输出到 `~/tavern-novels/<world>-<timestamp>.md`。这会调用一次 LLM,把你的完整 turn 日志重写成一篇连贯的小说,可分享。

### 建议

```
/hint               刷新当前建议列表(迷茫时随时看新主意)
/hint on|off        打开 / 关闭建议列表(默认开;永久关闭可在 config.toml 写 [ui] suggestions = false)
```

### 系统

```
/help              查看所有指令
/quit              退出
```

---

## 七、快捷键

在输入行里可用:

| 快捷键 | 作用 |
|---|---|
| `↑` `↓` | 翻历史输入 |
| `Ctrl+C` | 打断当前 GM 输出,立即显示已生成的完整文本 |
| `Space` | 长文分页翻页 / 跳过打字机效果 |
| `Ctrl+L` | 清屏(不清历史) |
| `Ctrl+R` | 搜索历史输出 |
| `Ctrl+P` | 命令面板(所有 `/` 指令的可视选择) |
| `Ctrl+D` / `/quit` | 退出 |

---

## 八、死亡

HP 归零 = **game over**,当前进度不能继续。

```
        你已在这个世界里陨落。

        [R] 读取存档       [T] 回到初始酒馆(重开一局)       [Q] 退出
```

- **[R] 读档**:回到最近的存档点,故事从那里继续。
- **[T] 回到酒馆**:同一个世界、同一个 PC(状态重置),从初始酒馆重开。**上一次的死亡在这个世界里真实发生过**,可能作为传闻/回声在后续出现。
- **[Q] 退出**:关掉引擎,存档保留。

**没有廉价复活**。想不死就多存档,或者别做太蠢的选择。

---

## 九、剧情节奏

Tavern 的剧情不是从一开始就"跌宕起伏"。系统采用**渐进推进**:

- **蜜月期**(开局前 ~20 回合):你自由探索,认识 NPC,熟悉世界。系统不主动推剧情。
- **轻推期**(~20 ~ ~50 回合):偶尔出现小事件、小转折,你会感觉世界"活起来了"。
- **策划期**(~50 回合之后):主线开始成形,伏笔被激活,冲突升级。

具体阈值由**世界包**配置,一个"高强度冒险"世界可能 5 回合就进策划期,一个"日常慢生活"世界可能 100 回合都在蜜月期。

---

## 十、目录结构

Tavern 用到的所有文件:

```
~/.config/tavern/
├── config.toml       # LLM 密钥和 UI 偏好
├── worlds/           # 已安装的世界包
│   ├── my-jianghu/
│   └── my-cthulhu.toml
└── saves/            # 存档目录,每份存档一个 .db 文件
    ├── slot-1.db
    └── my-mage.db

~/tavern-novels/      # /export novel 的默认输出目录
```

存档可以直接备份 —— 复制 `.db` 就行。想分享给朋友让他从你的进度接着玩,把 `.db` 给他就是。

---

## 十一、疑难解答

### GM 老是忘记之前发生的事
- 检查 `/journal`,看看关键事件有没有被记录
- 如果玩了很多回合、场景切换多次,`/wiki <NPC 名字>` 看看这个 NPC 的记忆条目是否完整
- 如果发现记忆确实丢了,可能是你的模型上下文窗口不够,考虑换个大 context 的模型

### NPC 说话前言不搭后语
- 通常是模型选得太小。Narrator 建议至少用 Sonnet / GPT-4 级别
- 也可能是这个世界包的 NPC 人物卡写得不够清楚,可以看看 `WORLD_BUILDING.md` 了解怎么写好一张卡

### 想让 GM 换个语气
不要在游戏里跳出来跟 GM 说"请更文艺一点"—— **那样会破坏沉浸感,而且 GM 可能不会真的听你的**。
正确做法:退出游戏,编辑 `~/.config/tavern/worlds/<world>/world.toml`,调整 `tone` 或 `style` 字段。

### /export novel 太贵
小说导出会把你的完整 turn 日志喂给 LLM 重写,费 token。可以在 `config.toml` 里为 `export` 单独指一个便宜模型,或者定期导出而不是等到几十小时后一次性导。

### 完全没反应 / 报错
- 检查 API key 是否有效、余额是否够
- 检查 `~/.config/tavern/config.toml` 语法是否正确(用 `tavern config check`)
- 用 `tavern --debug` 启动看详细日志
- 还不行,提 issue:<repo-url>/issues

---

## 十二、下一步

- 想造一个属于自己的世界?看 **`WORLD_BUILDING.md`**
- 想了解引擎在做什么、想贡献代码?看 **`DESIGN.md`**
- 想分享一个精彩的故事?导出小说,发到项目讨论区

祝你在酒馆里,遇到好故事。
